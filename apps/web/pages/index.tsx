import { NextPage } from "next";
import { Button } from "ui";

const Homepage: NextPage = () => {
  return (
    <div>
      <p>Hello, world!</p>
      <Button />
    </div>
  );
};

export default Homepage;
